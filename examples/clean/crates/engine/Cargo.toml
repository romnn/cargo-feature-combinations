[package]
name = "engine"
version.workspace = true
edition.workspace = true
publish.workspace = true

[features]
metrics = []
tracing = []

# Extra metadata merged into every row of `cargo fc matrix`.
[package.metadata.cargo-fc.matrix]
kind = "lib"
ci = true
