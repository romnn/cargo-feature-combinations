[package]
name = "cli"
version.workspace = true
edition.workspace = true
publish.workspace = true

[features]
color = []

[dependencies]
engine = { path = "../engine" }

[package.metadata.cargo-fc.matrix]
kind = "bin"
ci = true
